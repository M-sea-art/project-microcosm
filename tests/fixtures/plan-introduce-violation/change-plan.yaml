change:
  id: change.introduce-violation
  description: Add a direct write that the policy forbids.
  actions:
    - kind: add_edge
      edge:
        id: edge.executor-memory
        from: agent.executor
        to: memory.long-term
        relation: writes
        attributes: {}
        evidence_refs: ["evidence.plan.executor-memory"]
