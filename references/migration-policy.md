# Migration Policy

V0.x schemas may evolve. Each breaking change must upgrade schema_version and provide migration guidance.

Do not silently guess old schemas. Do not overwrite original snapshots. Stop on incompatible version, report the difference, and recommend a migration command.
