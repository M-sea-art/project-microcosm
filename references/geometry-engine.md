# Geometry Engine

V0.1 supports six deterministic invariants: forbidden-edge, required-path, single-writer, no-cycle, max-fanout, permission-subset.

Geometry checks compare Observations against Assertions and Policies. They do not mutate project source.
